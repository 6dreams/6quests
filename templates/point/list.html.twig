{% extends "base.html.twig" %}

{% block title %}{{ parent() }} - Список точек{% endblock %}

{% block body %}
    <h2>Список точек [<a href="{{ path('coord_index') }}">Обновить</a>]</h2>
    {% for point in points %}
        <li><a href="{{ path('point_view', { 'id': point.id }) }}">{{ point.name }}</a></li>
    {% else %}
        На данный момент, нет точек в которых вы можете учавствовать.
    {% endif %}
{% endblock %}
